# 0.3.2 問題定位與修正說明

## 結論

本次看到的「中文字重疊、沒有對準對話框、字體忽然縮小、對話框留下大量空白、原文沒有的線條」不是五個互不相干的問題，而是同一條處理鏈的幾個失控點：

1. 同一段原文被整句框與欄位碎片重複 OCR。
2. 翻譯收到重複內容後變得異常冗長。
3. 舊排版器只負責把字塞進矩形，放不下就機械式縮小。
4. 矩形來自偵測框或文字 mask 的緊密框，並不等於完整對話框的可用空間。
5. 排版是在擦除後才確定；就算字太小或會重疊，原文也已經被清掉。
6. 日文長音符 `ー` 被模型轉成中文破折號，直排後看起來像原圖多出線條。

0.3.2 將策略改為：**先選出單一完整 OCR，重建原文字塊幾何，在擦除前完成可讀排版預演，通過後才局部修復並按同一份計畫寫回。**

---

## 一、舊版為什麼會自動縮小字體

### 舊機制確實是機械式 fit

舊排版器大致執行以下步驟：

```text
取得一個矩形
→ 以某個最大字級嘗試排版
→ 放不下就減小字級
→ 重複直到 fit
→ 若仍放不下，把剩餘文字塞進最後一欄
```

這種作法在上游資料完全正確時勉強可用，但它不理解「原字有多大、原本有幾欄、文字塊中心在哪裡、對話框內還有多少安全留白」。因此只要發生任何一項偏差：

- detector 框比原文字略窄。
- 翻譯比日文長。
- 同一句被 OCR 兩次。
- 大框與欄位碎片被串在一起。
- `font_size_max` 比高解析度原字小。

最後都會被轉化成同一個表面現象：字級一路縮小，直到塞得下。

### 為什麼會出現小字與大片留白同時存在

「字很小」不代表排版框很小。舊流程有時以大 group bbox 決定置中，卻以另一個較緊的尺寸決定 fit；或翻譯先因重複而被壓到很小，清洗／截斷後內容又變短。結果就是中文字縮成一小團，周圍卻仍有大片空白。

---

## 二、0.3.2 的原幾何重建

### 1. 原字級不再只相信單一 detector hint

`_infer_original_geometry()` 綜合：

- 主尺寸 detector 的 `font_size_hint`。
- 真實文字 mask 在 X／Y 軸上的投影厚度。
- 原文字欄位中心與間距。
- 原文字像素外接框。
- OCR 原文字數。

若 detector hint 與 mask 投影差距極端，會拒絕不合理 hint，避免把 40 px 原字誤估成 130 px 或反過來。

### 2. 重建的不只是 bbox

每個群組會得到：

```text
OriginalTextGeometry
  bbox            原文字像素實際範圍
  font_size       原字級估計
  primary_count   原直排欄數或橫排行數
  primary_step    原欄距／行距
  secondary_step  原字距
  source_length   原文可見字數
```

因此排版目標不再只是「塞進某個框」，而是盡量重現原文字塊的字級、中心、寬度與高度。

### 3. 先找同一對話框內的安全空間

`_safe_background_bbox()` 會從原文字區向外搜尋近似純色的字幕框／對話框底色，並以背景亮度、主色比例、色差、最大無效像素比例與內邊距限制擴張。

這不是盲目擴大 bbox：

- 搜尋區只在原文字附近。
- 文字核心 mask 會先排除。
- 只有近似一致底色才允許擴張。
- 遇到人物、線稿、框線或深色背景會停止。

因此較長譯文會先利用原對話框既有留白，而不是先縮字。

---

## 三、字級選擇不再由單一 fit 結果決定

### 兩階段選擇

`_choose_layout_candidate()` 先把所有可行方案分成：

1. **原尺寸保留帶**：字級至少為原字級的 92%。
2. **有限縮字帶**：只有保留帶完全沒有可行方案時才可使用。

只要第一類存在，第二類即使幾何分數稍好也不會被選中。這解決了舊版「明明原尺寸排得下，演算法卻因為小字塊更接近某個目標比例而選小字」的問題。

### 預設最低可讀門檻

```yaml
typesetting:
  font_preserve_floor_scale: 0.92
  min_font_scale: 0.85
  reject_unreadable_layout: true
```

預設行為：

- 原尺寸附近可行：維持至少 92%。
- 原尺寸附近完全不可行：可有限縮到約 85%。
- 85% 仍排不下：標記 `layout_rejected`，保留原日文。

`hard_min_font_scale` 只在明確關閉 `reject_unreadable_layout` 的相容模式下使用；預設流程不會一路縮到 4 px。

---

## 四、如何填滿原本空間，而不是留下大面積留白

### 直排文字

`_plan_vertical()` 會同時搜尋：

- 可讀字級。
- 欄數。
- 每欄字數分配。
- 字距。
- 欄距。
- 文字塊中心。

文字會採平衡分欄，不再把前幾欄塞滿、最後一欄只剩一兩字；字距與欄距會在設定範圍內向原文字塊寬高靠近。

翻譯較短時，系統優先：

1. 維持原字級。
2. 置中於原文字中心。
3. 在合理範圍增加字距／欄距。
4. 視文字數保留接近原欄數的分欄。

翻譯較長時，系統優先增加欄數並利用安全留白，之後才考慮有限縮字。

### 橫排文字

`_plan_horizontal()` 會依原寬度加入有限 tracking，並以原文字高度調整行距；短譯文不再只是貼在原框中央的一小段。

### 為何不保證每一段都百分之百填滿

若日文 12 個字翻成中文 4 個字，在完全維持原字級的前提下，不可能同時精確複製原寬、原高與原密度。新版優先順序是：

```text
可讀字級 > 原位置與中心 > 合理字距／欄距 > 原佔用範圍 > 強行拉滿
```

因此不會用誇張字距或任意放大字體來製造「滿版」，但會避免舊版那種明顯的小字縮在角落。

---

## 五、重疊的真正根因：整句 OCR 與碎片 OCR 被串接

### 實際偵測型態

同一段直排文字在多解析度 detector 中可能同時出現：

```text
完整對白大框
欄 1 小框
欄 2 小框
上半句框
下半句框
另一解析度的近似框
```

舊 `_combine_region_candidates()` 可能把 whole-region OCR 與 leaf-column OCR 接在一起。即使每個 OCR 單獨都合理，組合後仍會形成：

```text
完整原文 + 上半句 + 下半句
```

翻譯模型接到的就是重複文字，所以最終不是單純兩個 render layer 重疊，而是一段譯文內部已經重複。

### 修正

- whole-region OCR 與 leaf-column OCR 改為替代假設，不再串接。
- mask 高包含率可辨識「大框包含細欄」，即使 IoU 因面積差異很低。
- `_best_ocr_group()` 優先完整內容，不會因碎片 confidence 高 0.02 就捨棄整句。
- OCR 後與翻譯後都會做巢狀內容去重。
- 真正分離的重複語氣仍保留，不會把合法重複全部刪掉。

---

## 六、位置沒有對準對話框

舊位置偏移通常不是單純 X／Y 座標錯誤，而是以下連鎖：

1. OCR 串入重複碎片，譯文變長。
2. 直排從右向左新增欄位。
3. 緊框無法容納，字級縮小或欄位向一側堆積。
4. 視覺上看起來像整段文字偏離原對話框。

0.3.2 以原文字塊中心作為 target center，擴張後仍將文字 block 拉回原中心；若實際 block 因邊界限制必須偏移，也會限制在安全 bbox 內。

另外，排版所使用的 `layout_bbox` 與真正畫出的 `block_bbox` 都寫入 debug manifest，方便區分「安全可用區域」與「實際文字佔用區域」。

---

## 七、排版預演與實際 block 碰撞

舊流程在 inpaint 後才發現排版問題，已經來不及保留原文。0.3.2 新增 `_preflight_layout_plans()`：

1. 在原圖上替所有有效譯文產生 `TextLayoutPlan`。
2. 不可讀方案直接拒絕。
3. 比較每份 plan 的實際 `block_bbox`。
4. 若與已接受譯文碰撞，先嘗試不擴張對話框的 compact plan。
5. 仍碰撞則標記 `layout_collision_rejected`。
6. 只有接受的 plan 才允許 inpainting。
7. 寫回時重用同一份 plan，不再二次隨機或重新計算。

因此排版失敗不會產生空白框，也不會因前後兩次計算不同而出現不可重現的位置。

---

## 八、「謝謝指導」下方線條的來源

原文：

```text
ありがとうございましたーッ
```

其中 `ー` 是日文長音符。翻譯模型可能輸出：

```text
謝謝指導——！
```

直排時 `——` 便看起來像兩條額外水平線。這不是 inpainting 殘影，而是譯文字符。

### 修正

- prompt 明確禁止把日文長音符轉成破折號。
- `sanitize_translation_text()` 會依原文判斷：原文只有長音符而沒有真正 dash 語意時，移除譯文中的 `—―─━–－﹘﹣⸺⸻` 等線條字元。
- 若原文本來就有真正破折號語意，則保留。

---

## 九、平坦背景殘影與過度擦除

### 舊風險

- detector mask 往往只覆蓋黑色字形核心，灰色反鋸齒邊緣留下淡淡原文。
- 直接大幅膨脹 mask 雖可清除殘影，卻容易吃到人物、髮絲、框線或背景細節。
- 意外尺寸的 local mask 若被當成 full-page mask，可能放大成整頁破壞。

### 修正

1. 只有 mask 尺寸與整頁完全相符時才視為 full-page mask。
2. 其他尺寸一律限制在 group bbox 內重新對齊。
3. `hybrid` 先判斷周圍是否為近似純色背景。
4. 只有平坦背景才在 3 px 內補抓與背景有足夠色差的反鋸齒邊緣。
5. 新 mask 的像素增長受 `hybrid_flat_edge_max_growth` 限制。
6. 以小半徑 Telea 逐群組修復，不使用整個框的單一中位色覆蓋。

這能清除淡灰字形殘影，同時不重新引入大片模糊。

---

## 十、保留的安全原則

- 沒有可靠文字像素 mask：不擦。
- OCR 不合格：不翻、不擦。
- 譯文格式或內容異常：不擦。
- group 幾何高度重疊：只保留較可靠者。
- 實際排版 block 重疊：拒絕較弱者。
- 需要低於可讀字級：保留原文。
- 整頁未預期錯誤：輸出原圖，避免缺頁。

---

## 十一、OCR runtime 相容性（沿用 0.3.1）

舊 `manga-ocr==0.1.11` 的 `AutoFeatureExtractor` 與目前 Transformers 不相容，會拋出 `Unrecognized feature extractor`；初始化失敗後又被每個文字框重試，最後仍顯示「失敗 0 頁」。

現行實作：

- `ViTImageProcessor`
- 明確日文 BERT tokenizer
- `VisionEncoderDecoderModel`
- 成功與失敗狀態都快取
- 批次處理前 OCR preflight
- 致命初始化錯誤直接由 CLI 回報一次

---

## 十二、客觀限制

- 原文覆蓋的人物線條本來就不存在於輸入圖，OpenCV 無法憑空還原；本版只承諾縮小修復範圍，不宣稱重建真實隱藏內容。
- raw segmentation 完全漏字時，保守 mask 無法憑空產生文字像素；應增加偵測尺寸或檢查 debug，而不是常駐開啟矩形 fallback。
- 翻譯若極端冗長且無法在 85% 原字級以上排入，預設會保留日文。這比輸出不可閱讀小字或擦成空白更安全。
- 自動翻譯仍可能有語意問題；本版重點是幾何、可讀性、標點與影像安全。
