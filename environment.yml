name: manga
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  # Conda only owns the interpreter and the approved Python package manager.
  # Project/runtime dependencies are resolved from poetry.lock by Poetry.
  - poetry=2.2.1
