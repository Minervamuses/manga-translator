# 人工核准 SVG 流程行動方向計畫

## 1. 文件定位

本文件是供共同審核的行動方向計畫，定義要解決的問題、目標狀態、必要優化項目、推進順序、
審核門檻與停止條件。它不是落地計畫，也不決定函式、介面、資料欄位、座標記錄方式、目錄
結構、第三方函式庫或其他實作細節。

本文件通過審核後，下一步只撰寫對應的落地計畫；落地計畫再次通過審核後才開始實作。後續
每一階段都遵守相同方式，不因前一階段獲准而自動取得擴大範圍的權限。

## 2. 背景與方向決策

`main` 已能穩定出圖，視覺效果也是目前基準，但自動排版、錯配與失敗復原仍有風險。`repair`
加入較多安全與追蹤能力，歷史結果也顯示：繼續把字體、字級、位置、留白與畫面美感寫成更多
自動門檻，會增加複雜度，仍不一定得到人類認可的結果，甚至可能讓大量文字區完全不寫回。

因此後續方向不是繼續擴張自動美術判定，而是重新分配責任：

- 機器負責偵測、OCR、翻譯、初始排版、來源對應、原文處理、安全檢查與確定性合成。
- 人類負責字體、字級、顏色、方向、位置、間距、旋轉、整頁閱讀感與最終視覺核准。
- 自動排版降為可編輯初稿，不再作為文字能否進入人工處理的完成門檻。
- 人工核准的 SVG 結果由程式依原頁位置合成，不再交由模型重新猜測放置位置。
- 未核准、無法驗證或合成失敗的區域維持來源像素。

## 3. 目標狀態

完成這個方向後，一頁漫畫的理想流程是：

1. 現有流程產生文字區、OCR、譯文、初始排版與固定擦除範圍。
2. 每個文字區成為保有原頁位置與來源關係的 SVG 物件。
3. 同頁物件整理成可在完整頁面脈絡中編輯的頁面 SVG。
4. 人類使用既有 SVG 編輯環境調整視覺效果，逐物件核准或選擇保留原文。
5. 系統確認頁面、物件、來源、字型與核准內容仍一致。
6. 系統從原圖開始，只對已核准物件進行原文處理與 SVG 合成。
7. 個別物件失敗時保留該區原圖，其他已核准物件仍可完成。
8. 人類查看整頁結果，確認局部排版與整體閱讀效果。

在這個目標狀態取得足夠證據以前，`main` 持續作為視覺品質與完成率基準，不直接被新流程
取代。

## 4. 核心設計原則

### 4.1 視覺判斷由人類負責

- 系統提供初稿、限制與警示，但不以更多審美規則推翻人工核准。
- 人工核准後若存在安全問題，系統應明確退回，而不是靜默縮字、裁切、換字型或改位置。
- 自動排版無法產生可接受初稿時，仍須產生可供人工處理的基本文字物件。

### 4.2 頁面位置與物件身分必須可靠

- SVG 物件直接保有原頁位置，不需要在回灌時重新定位。
- 每個物件與來源文字區保持明確關係，不依列表位置或當次顯示順序猜測配對。
- 物件遺失、重複、互換或來源不一致時，只拒絕受影響物件，不進行模糊補配。
- 人工編輯時保留整頁脈絡，避免只看裁切小圖而忽略跨物件與閱讀效果。

### 4.3 擦除與文字覆蓋分離

- 人工調整 SVG 不得暗中擴大原文擦除範圍。
- 原文擦除必須等到相應的人工核准文字可安全處理後才發生。
- 允許改變的像素只來自已核准的固定擦除範圍與人工核准文字的可見範圍。
- 未核准文字區及其他非目標區域不得因相鄰物件成功而被修改。

### 4.4 逐物件失敗隔離

- 核准、驗證、原文處理與合成都以文字物件為最小失敗單位。
- 單一物件失敗不拖垮其他已核准物件，也不能留下已擦除但沒有譯文的空白區。
- 部分完成頁面必須如實呈現哪些物件已完成、哪些保留原文，不能包裝成完整成功。

### 4.5 SVG 往返必須受控且可重現

- 原型與初期導入只支援經確認的既有 SVG 編輯環境與字型環境。
- 只接受本方向實際需要且能驗證的 SVG 能力；外部、動態或不可控內容不得進入合成。
- 人工工作版本與最終核准結果的角色必須明確，避免編輯器自動改寫造成未察覺的視覺差異。
- 同一核准成果在受控條件下應得到一致的最終畫面。

### 4.6 以實際輸出證明方向

- 優先使用既有真實頁面、OCR、譯文、遮罩與輸出，不先建立新的 benchmark 或測試框架。
- 先驗證最小代表頁面，通過後才擴大到其他既有代表案例。
- 實際漫畫頁的視覺結果、原圖保護與人工成本是主要證據，階段狀態或代理指標不能取代它們。
- 驗證期間不重新呼叫付費翻譯服務。

## 5. 核心優化項目

### 5.1 頁面級 SVG 與文字物件

- 將文字區轉成保有原頁位置與來源關係的 SVG 物件。
- 將同頁物件整理成具完整頁面脈絡的單一編輯成果。
- 沿用現有譯文與排版結果產生初稿，排版拒絕時仍提供基本可編輯內容。
- 讓來源、譯文、人工成果與最終合成之間維持足夠的可追溯關係。

### 5.2 人工編輯與核准流程

- 使用現有 SVG 編輯器，不先自製專用編輯器。
- 讓人工能調整本方向要求的基本視覺屬性，並隨時對照原圖與整頁效果。
- 每個文字物件都能明確選擇核准或保留原文。
- 逐物件核准後仍保留整頁完稿檢查，避免局部正確但整頁閱讀效果失衡。
- 不支援的編輯結果需提供明確原因，讓人工能修正或保留原文。

### 5.3 受控 SVG 與字型一致性

- 明確區分可自由編輯的工作成果與可安全合成的核准成果。
- 限制進入正式合成的 SVG 能力，排除會破壞安全性或可重現性的內容。
- 控制字型與渲染條件，避免換環境後發生字型替換或跑版。
- 驗證既有編輯器開啟、修改及儲存後，文字物件仍能正確辨認與合成。

### 5.4 安全原文處理與合成

- 每次都從不可變原圖開始，不在前一次輸出上累積修改。
- 固定擦除範圍與 SVG 文字覆蓋分開處理，各自接受驗證。
- 只有已核准且能完整處理的文字物件才允許擦除原文。
- 每個物件先完成驗證、原文處理、合成與結果檢查，再套用到整頁。
- 確認未核准區域與核准範圍以外的像素沒有被修改。

### 5.5 成果判定與實際比較

- 將完整完成、部分完成、完全保留原圖與真正失敗分開判斷。
- 使用相同頁面的 `main` 結果作為視覺與完成率基準。
- 記錄人工排版所需時間，確認品質改善是否值得增加的人工作業。
- 分別檢查正常文字區、自動排版拒絕區及刻意失敗區的實際結果。
- 只有真實頁面結果通過，才允許進入下一階段規劃。

## 6. 推進階段與審核門檻

### 階段 0：方向計畫審核（本文件）

目標是確認責任分界、核心原則、優化範圍、推進順序與待決策事項是否正確。

審核通過條件：

- 人類與系統的責任分界獲得認可。
- 必做項目與明確不做項目沒有混淆。
- 安全原則沒有因追求完成率而被削弱。
- 審核者同意下一步只撰寫落地計畫，不直接實作。

### 階段 1：撰寫落地計畫

落地計畫才決定如何接入現有流程、各成果的角色與關係、物件身分策略、核准方式、可接受的
SVG 範圍、字型與渲染條件、合成邊界及針對性驗證方式。

審核門檻：落地計畫必須證明所選方案是現有程式中最小、可驗證的改動，且沒有建立不必要的
平行 pipeline、持久資料層或通用框架。未通過審核前不修改 production code。

### 階段 2：最小端到端實作

只完成一張既有真實頁面所需的 SVG 匯出、人工編輯、核准、匯入與逐物件安全合成。沿用既有
OCR、譯文、遮罩與 `main` 結果，不同時處理其他上游問題。

審核門檻：未修改 SVG、基本視覺調整、自動排版拒絕、保留原文、錯誤物件及單一物件失敗等
必要案例都能得到符合原則的實際頁面結果。

### 階段 3：代表性頁面試用

最小實作通過後，先依實際結果撰寫代表性頁面試用計畫並交付審核；通過後才使用少量既有
代表性頁面，確認直排文字、不同字量、不同背景與常見人工調整不會破壞物件配對、原圖安全
與整頁品質。

審核門檻：相較 `main` 沒有不可接受的完成率或視覺退步，安全事故為零，人工時間可接受，且
沒有出現必須先重構其他系統才能繼續的證據。

### 階段 4：正式流程整合計畫與實作

只有代表性試用通過後，才另寫正式整合計畫；計畫通過審核後才實作。方向上應沿用現有
pipeline，替換或插入人工核准邊界，而不是長期維護兩套重複流程。`main` 在新路徑取得充分
證據前仍保留為回退基準。

審核門檻：正式整合不破壞既有出圖能力，失敗仍保留來源像素，成果狀態如實呈現，且所有新增
範圍都已經由新的落地計畫明確批准。

### 階段 5：是否擴大使用的決策

根據實際品質、完成率、安全性與人工成本，決定停止、維持選用模式，或另行規劃更廣泛使用。
任何預設啟用、取代 `main`、擴充編輯能力或支援更多工具的工作，都需要新的明確批准。

## 7. 主要風險與應對方向

| 風險 | 應對方向 |
|---|---|
| 編輯器重新整理 SVG 後物件無法辨認 | 在落地計畫中定義最小穩定物件關係，並先做實際往返驗證 |
| 字型替換或渲染差異造成核准結果跑版 | 限定首批環境，並明確區分工作成果與最終核准成果 |
| 自動排版失敗導致沒有內容可交給人類 | 無論初稿是否成立，都輸出基本可編輯文字物件 |
| SVG 編輯意外擴大原文擦除範圍 | 擦除範圍與文字覆蓋分離，任何擴張另行批准 |
| 單一物件失敗後留下空白或破圖 | 擦除前確認核准文字可處理，失敗時保留來源像素 |
| 人工文字覆蓋影響未核准區域 | 以整頁脈絡核准，並檢查最終差異只落在允許範圍 |
| 人工成本高於視覺改善價值 | 在最小實作與代表性試用階段實際記錄時間並設停止點 |
| 原型逐步膨脹成另一套複雜 pipeline | 每階段重新寫落地計畫並審核，不自動延伸到下一階段 |

## 8. 本方向目前不包含

- OCR、譯文、閱讀順序、文字區拆分與合併的重新設計。
- 自製 SVG 編輯器、風格預設系統、自動學習或通用排版框架。
- 中斷續跑、重跑、重播、跨版本編輯重用或新的持久資料層。
- 人工擴大擦除範圍；若未來需要，必須作為獨立方向重新審核。
- 圖片 metadata、發布封裝、依賴升級或其他既有但與本方向無直接關係的問題。
- 新 benchmark 框架、完整資料集 replay、廣泛參數搜尋或付費 API 測試。
- 在本文件中決定函式、介面、資料欄位、座標格式、目錄結構或具體技術選型。

## 9. 進入落地計畫前的待決策事項

以下決策涉及成果角色、格式邊界或正式行為，必須由使用者與審核者明確確認：

1. SVG 是人工交換與編輯成果，或也是長期保存的權威成果。
2. 最終核准成果保留可編輯文字，或固定為不再受字型環境影響的外觀。
3. 第一個落地版本使用哪一組既有 SVG 編輯環境與字型環境。
4. 核准以逐物件為主並加整頁完稿，或需要不同的人工核准層級。
5. 人工移動文字超出原文字區時，哪些情況只需警示，哪些情況必須拒絕。
6. 第一個落地版本是否只允許視覺調整，將文字內容修訂留待另一個方向審核。
7. 現有 `main` 與 `repair` 中哪些安全能力可直接沿用，哪些應避免帶入新流程。

## 10. 本文件通過後的唯一下一步

若本方向計畫通過審核，只撰寫「最小端到端 SVG 流程落地計畫」。該文件需對應本計畫的必要
原則與階段 2 範圍，提出最小改動、針對性驗證及需要使用者批准的技術決策；完成後再次停下
等待審核，不直接開始實作。
