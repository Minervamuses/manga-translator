# 人工核准 SVG 原型計畫

## 目的

用一張既有真實漫畫頁驗證最小的人工 SVG 往返流程：系統把文字區整理成保有原頁位置的
SVG 物件，人類調整視覺排版，系統再把已核准成果安全合成回原圖。

本計畫只做到可判斷方向是否成立的原型。驗證完成後即停止並回報，不預先規劃正式導入、
取代既有流程或其他後續擴張。

## 必要原則

- 沿用現有偵測、OCR、譯文、擦除範圍與 `main` 輸出，不重構上游流程。
- 每個文字區塊成為保有原頁位置與來源關係的 SVG 物件；同頁物件整理在同一頁面 SVG 中。
- 自動排版只提供初稿；初稿失敗不得阻止文字物件交由人工處理。
- 人類負責字體、字級、顏色、方向、位置、間距、旋轉與最終視覺判斷。
- SVG 文字覆蓋與原文擦除範圍分離；原型期間擦除範圍固定，不因人工編輯自動擴張。
- 未核准、無法驗證或合成失敗的文字物件保留來源像素。
- 原型只使用一組由使用者確認的既有 SVG 編輯環境與字型環境。

## 執行步驟

### 1. 準備最小樣本

- 從既有驗證材料選一張真實頁面與少量具代表性的文字區。
- 重用既有 OCR、譯文、遮罩與輸出，不建立新測試框架，也不呼叫付費服務。
- 保留 `main` 的現有結果作為同頁視覺比較基準。

### 2. 產生並人工編輯頁面 SVG

- 將選定文字區輸出成可在完整頁面脈絡中編輯的 SVG 物件。
- 保持每個 SVG 物件與來源文字區的最小且明確對應。
- 提供現有排版結果作為可編輯初稿；沒有可用初稿時仍提供基本文字物件。
- 由人工完成基本視覺調整，並逐物件決定核准或保留原文。

### 3. 安全合成回原圖

- 匯入時確認頁面、文字物件與來源區域仍正確對應。
- 只接受原型所需且可驗證的 SVG 內容；不支援的內容明確退回，不靜默裁切或改寫。
- 每次從原圖開始，依固定擦除範圍逐物件處理與合成。
- 單一物件失敗只影響自身；其他物件可繼續，失敗區域維持來源像素。
- 確認擦除範圍外與其他未核准區域沒有被修改。

### 4. 驗證後停止

- 未修改的頁面 SVG 能正確往返並回到原位置。
- 人工調整的基本視覺屬性能保留，且不影響其他文字物件。
- 遺失、重複、互換或來源不一致的文字物件能被發現並拒絕。
- 被自動排版拒絕的文字物件仍能經人工處理後安全合成。
- 未核准或失敗區域保持原圖，非目標像素沒有改動。
- 對照 `main` 的實際頁面結果並記錄人工處理成本，回報原型是否值得繼續。
- 完成以上驗證後停止；任何擴大範圍的工作另行取得使用者同意。

## 本版不包含

- 中斷續跑、重跑、重播、跨版本編輯重用或新的持久資料層。
- 正式流程整合、批次導入、取代 `main` 或建立平行 pipeline。
- 文字區拆分與合併、OCR 或譯文修訂、閱讀順序調整。
- 自製 SVG 編輯器、多編輯器相容、字型擴充、風格預設或自動學習。
- 人工擴大擦除範圍、圖片 metadata 改善或其他既有圖片處理問題。
- 新 benchmark、完整資料集 replay、廣泛參數測試或付費 API 測試。
- 函式、介面、資料欄位、座標格式、目錄結構或具體技術選型。
