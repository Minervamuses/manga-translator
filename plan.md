# 人工核准向量文字流程優化計畫

## 文件定位

本計畫描述下一階段的優化方向、推進順序與驗收原則。這一版只回答「要改善什麼」與
「何時算完成」，暫不決定函式、介面、資料欄位、座標表示、目錄結構、第三方函式庫或其他
實作細節。

## 背景與方向

`main` 目前仍是可穩定出圖且視覺效果較成熟的基線，但存在翻譯錯配、失敗狀態誤報、缺乏
重播能力，以及寫回後無法逐區安全復原等風險。`repair` 已補強持久狀態、嚴格配對、逐區
寫回與失敗保護，但全自動排版及安全判定逐漸複雜，歷史上也曾使大量文字區無法完成寫回。

後續主方向改為「人工核准的向量文字流程」：

- 機器負責偵測、OCR、翻譯、初始草稿、原文處理、安全驗證與確定性合成。
- 人類負責字體、字級、顏色、方向、位置、間距與整頁視覺判斷。
- 自動排版器降為草稿產生器，不再以自動美術判定作為主要完成門檻。
- 保留 `repair` 中可續跑、可重播、可稽核、逐物件失敗隔離及保留原圖的能力。
- 在新流程取得充分證據前，以 `main` 作為完成率與視覺品質基線。

## 核心原則

1. **頁面位置由資料直接保留。** 人工編輯後的文字不再交由模型重新猜測應放回何處。
2. **視覺決策交給人類。** 系統提供可用草稿與警示，不以更多門檻取代人工美術判斷。
3. **原圖永遠可回復。** 任一文字物件未核准、無效或合成失敗時，該區保留來源像素。
4. **擦除與文字覆蓋分離。** 調整文字外觀或位置不得暗中擴大原文擦除範圍。
5. **逐物件核准與提交。** 單一物件失敗不拖垮其他已通過物件，也不把整頁誤報為完整成功。
6. **結果必須可追溯、可重播。** 原圖、辨識、譯文、人工編輯、核准與輸出之間保持明確關係。
7. **先證明小範圍往返，再擴大自動化。** 優先驗證編輯與合成鏈路，不先自製大型編輯工具。

## 優化項目與推進順序

### P0：固定基線與驗證方法

- 保留目前可用的 `main` 出圖流程，作為新流程的對照基準與回退方案。
- 建立不需再次呼叫付費翻譯服務的代表性驗證材料。
- 分開衡量流程是否順利執行，以及內容是完整、部分完成、未修改或失敗。
- 建立文字物件從偵測到最終寫回的成果漏斗，避免只以「整批執行成功」代表實際完成。
- 將視覺品質、正確配對、完成率、原圖保護、可重現性與人工處理成本納入共同驗收。

### P1：建立頁面級向量編輯成果

- 將每個文字區塊整理成保有原頁位置關係的 SVG 物件。
- 將同頁文字物件組成可在完整頁面脈絡中編輯的頁面級 SVG。
- 為每個文字物件建立不因排序或重跑而漂移的穩定身分，並能追蹤合併與拆分關係。
- 維持文字物件與來源區域、OCR、譯文、擦除範圍及版本歷史的可追溯關係。
- 將原圖、可編輯內容、必要參考資料與預覽整理成可交付人工處理的單頁工作包。
- 由現有自動排版產生可編輯初稿，讓人工可以從接近成品的狀態開始調整。

### P2：建立人工編輯與核准流程

- 讓人工可調整字體、字級、顏色、描邊、方向、位置、間距、旋轉與其他必要視覺屬性。
- 提供原圖、初稿與編輯結果的快速對照，保留整頁閱讀脈絡。
- 支援逐文字物件的待處理、已修改、已核准、退回與保留原文狀態。
- 將文字區拆分、合併、重排、恢復原文與要求擴大擦除範圍視為明確操作。
- 將人工修正 OCR、譯文或閱讀順序視為可追溯的內容修訂，而不只是外觀修改。
- 允許已核准物件先完成，未核准物件繼續保留原文，不要求整頁一次全部通過。
- 逐物件核准後仍進行整頁完稿檢查，處理跨物件與整體閱讀效果問題。
- 首階段優先使用成熟的 SVG 編輯器，不自製完整圖形編輯器。

### P3：建立安全且可重現的 SVG 往返

- 定義專案可接受的 SVG 能力範圍，阻止動態內容、外部依賴及不可控效果進入正式合成。
- 在匯入時確認頁面、物件、來源與核准版本仍一致，並識別遺失、重複、互換或未宣告變更。
- 將可自由編輯的工作版本與可穩定重現的核准版本分開管理。
- 固定最終結果所依賴的字型與渲染條件，避免換機器或換環境後跑版。
- 驗證直排中文、橫排文字、標點、備援字型與常用編輯器之間的往返一致性。
- 對不支援或無法驗證的內容採逐物件拒絕，避免單一問題使整頁失敗。

### P4：重整擦除與安全合成

- 將原文擦除範圍與 SVG 文字覆蓋完全分離，各自核准與追蹤。
- 人工移動或改寫文字時，不自動擴大擦除範圍；必要擴張必須另行預覽與核准。
- 每次正式輸出都從不可變原圖開始，避免反覆編輯累積畫質損失或殘留修改。
- 保留來源圖片的透明度、色彩描述、方向與必要圖片資訊。
- 逐文字物件完成原文處理、向量渲染、合成與結果驗證，全部通過後才提交該物件。
- 驗證非目標區域與未核准區域沒有被改動。
- 未核准、無效、越界或合成失敗的文字物件保留來源像素，並提供清楚的退回原因。

### P5：整合可續跑與可稽核工作流

- 將向量匯出、人工編輯、核准、匯入、合成與輸出納入可中斷續跑的正式流程。
- 保留嚴格的原文與譯文配對，並將單一項目問題隔離在該文字物件。
- 修正文字區合併、拆分或重新排序後可能造成的物件身分與安全範圍錯配。
- 分別記錄執行狀態與內容成果，不再把未修改頁面包裝成一般成功。
- 保存足以重播與追查的人工核准、版本及合成紀錄。
- 建立清楚的資料保存、清理與隱私原則，避免原圖、中間成果與翻譯回覆無限累積。

### P6：分階段驗證與導入

- 先用單頁、少量文字物件驗證完整人工往返流程。
- 再擴展到包含直排、橫排、彩色字、特殊字型、跨框與複雜背景的代表性頁面。
- 驗證常用 SVG 編輯器開啟與儲存後，頁面和文字物件仍能正確識別及合成。
- 與 `main` 在相同上游材料下比較完成率、視覺品質、錯配、原圖保護與處理時間。
- 先以實驗或選用模式導入，累積實際人工操作與失敗案例後再擴大使用。
- 只有在新流程證明不降低安全性、完成率與視覺品質後，才評估取代既有正式路徑。

## 階段驗收門檻

### 原型可行

- 未修改的頁面級 SVG 可以完整往返並回到正確位置。
- 常見人工視覺調整可以保留，且不影響其他文字物件。
- 物件遺失、重複、互換或來源不一致可以被發現。
- 未核准或失敗區域維持原始像素。

### 安全可用

- 擦除範圍之外及非目標區域不被修改。
- 正式輸出保留來源圖片必要的格式與顯示資訊。
- 不安全或不可重現的 SVG 內容不會進入正式合成。
- 單一物件失敗只影響自身，頁面結果會誠實標示為部分完成或未修改。
- 相同的核准內容與受控環境可以穩定重現相同結果。

### 可進入正式試用

- 直排、橫排及代表性漫畫排版均通過人工往返驗證。
- 文字與譯文錯配、擦除後無文字寫回、非目標區域修改等安全事故為零。
- 相較 `main`，完成率與人工評估的視覺品質沒有不可接受的退步。
- 人工處理成本、退回原因與未修改頁面均可量測，且不再被流程成功狀態掩蓋。

## 本版暫不處理

- 不規定函式、介面、資料欄位、座標格式、目錄結構或具體技術選型。
- 不自製完整 SVG 編輯器。
- 不繼續擴張全自動美術判定與排版門檻。
- 不立即從人工修改紀錄推導或套用新的自動排版規則。
- 不同時全面重構 OCR、翻譯模型、分鏡辨識或閱讀順序；僅保留與正確配對及工作流安全直接相關的改善。
- 不在首階段允許未受控的任意 SVG 功能。

## 後續待決策事項

- 所有文字物件都經人工核准，或在取得可靠可信訊號後只將低可信與不滿意物件送交人工處理。
- SVG 作為最終權威成果，或只作為人工編輯介面並另保存可重播的編輯意圖。
- 核准版本保留可編輯文字，或封存為固定外觀。
- 人工擴大擦除範圍是否納入第一階段原型。
- 第一階段正式支援的 SVG 編輯器、字型與直排文字情境。
